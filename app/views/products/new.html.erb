<!DOCTYPE html>
<html lang="en">
    <head>
        <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no">    
    </head>
    <body>
      
      <%= render '/common/navegacion' %>
      
      <div class="container-fluid">
        
          <%= form_for :product, url: products_path, method: :post, :html => {:class => "form-horizontal"} do |f| %>
            <div class="form-group">
              <label for="name" class="col-sm-2 control-label"><%= f.label :name %></label>
              <div class="col-sm-10">
                <%= f.text_field :name, class: "form-control"%>
              </div>
            </div>
            <div class="form-group">
              <label for="description" class="col-sm-2 control-label"><%= f.label :description %></label>
              <div class="col-sm-10">
                <%= f.text_field :description, class: "form-control"%>
              </div>
            </div>
    
            <div align="center" class="col-md-3 col-md-offset-4">
              <%= link_to products_path do %>
                <button type="button" class="btn btn-primary">Back</button>
              <% end %>
              <%= f.submit 'Save', :class => 'btn btn-primary' %>
            </div>
          <% end %>        

      </div>
  </body>
</html>