<!DOCTYPE html>
<html lang="en">
    <head>
        <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no">    
    </head>
    <body>
      
      <!-- Static navbar -->
      <nav class="navbar navbar-default">
          <div class="container-fluid">
            <div class="navbar-header">
              <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
              </button>
              <a class="navbar-brand" href="#">ShopCart</a>
            </div>
            <div id="navbar" class="navbar-collapse collapse">
              <ul class="nav navbar-nav">
                <li><%= link_to 'PRODUCTS', products_path %></li>
                <li><%= link_to 'CARTS', carts_path %></li>
                <li><%= link_to 'RECIPES', recipes_path %></li>
                <li><%= link_to 'CART PRODUCTS', cart_products_path %></li>
                <li><%= link_to 'RECIPE PRODUCTS', recipe_products_path %></li>
              </ul>
              <ul class="nav navbar-nav navbar-right">
              </ul>
            </div><!--/.nav-collapse -->
          </div><!--/.container-fluid -->
      </nav>
      
      <div class="container-fluid">
        
        
      <%= form_for(@cart_product, :html => {:class => "form-horizontal"}) do |f| %>
      
        <div class="form-group">
          <label for="name" class="col-sm-2 control-label"><%= f.label :cart_id %></label>
          <div class="col-sm-10">
            <%= collection_select(:cart_product, :cart_id, @carts, :id, :name, prompt: true) %>
          </div>
        </div>
        
        <div class="form-group">
          <label for="description" class="col-sm-2 control-label"><%= f.label :product_id %></label>
          <div class="col-sm-10">
            <%= collection_select(:cart_product, :product_id, @products, :id, :name, prompt: true) %>
          </div>
        </div>
        
        <div class="form-group">
          <label for="description" class="col-sm-2 control-label"><%= f.label :quantity %></label>
          <div class="col-sm-10">
            <%= f.number_field :quantity %>
          </div>
        </div>        

        <div align="center" class="col-md-3 col-md-offset-4">
          <%= link_to cart_products_path do %>
            <button type="button" class="btn btn-primary">Back</button>
          <% end %>
          <%= f.submit 'Save', :class => 'btn btn-primary' %>
        </div>
        
      <% end %>           
        
      </div>
  </body>
</html>